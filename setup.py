from setuptools import setup, find_packages
                    
setup(
    name="local-monitor-handler",
    version="0.1.0",
    packages=find_packages(),
    install_requires=['axela_devtools'],
    author="Kacper",
    description="Addon of type monitor",
)
